# Session: vessel modal everywhere (5 Aug)

User request: "οπου και να κανω κλικ στην εφαρμογη πανω σε πλοιο, πρεπει να ανοιγει modal πλοιου, που θα εχει ολες τις πληροφοριες"
Project knowledge: clicking a ship must open a resizable modal on the same page (no navigation); vessel data is not user-editable.

## Done so far
- client/src/components/VesselProductsTable.tsx (NEW): shared grouped Products table (Equipment/Consumables/Other,
  qty, unit price, line total, quota, SupplyBadge, expandable serials, "This vessel total" row) + vesselSupplySummary().
- client/src/components/VesselDetailDialog.tsx: now also renders the Products section (uses VesselProductsTable),
  above the invoices block. Already had info + 4 KPI cards + contracts badges + invoices. Uses ResizableDialogContent.
- client/src/pages/VesselDetail.tsx: refactored to reuse VesselProductsTable + vesselSupplySummary (removed inline table,
  removed Table/SupplyBadge/groupContractProducts/Fragment imports).
- client/src/contexts/VesselModalContext.tsx (NEW): VesselModalProvider + useVesselModal() -> { openVessel, closeVessel, vesselId }.
- client/src/App.tsx: VesselModalProvider wraps <Router/>; routes /vessels/:id and /ops/vessel/:id now Redirect to
  /vessels?vessel=<id> (Vessels page already reads ?vessel= to open the dialog). VesselDetail & OpsVesselDashboard lazy imports removed.

## Remaining
- Wire useVesselModal() into every vessel click target:
  - pages/ops/OpsContractDetail.tsx line ~759 (Vessels tab row) and ~998 (financials per-vessel row) -> currently navigate(`/vessels/${id}`)
  - pages/Invoices.tsx ~788 (vessel breakdown Link href=/vessels/${v.vesselId})
  - components/InvoicesTable.tsx (has its own local VesselDetailDialog + disableVesselDialog prop) - can switch to context
  - components/RecordDetailsPanel.tsx (local VesselDetailDialog at ~311)
  - pages/Vessels.tsx (local dialog + ?vessel= param) - keep working, may switch to context
  - ops pages showing vessel names: OpsAssets.tsx, OpsCertificates.tsx, OpsDashboard.tsx, GroupDetail.tsx, CustomerDetail.tsx (check)
- Also delete/retire pages/VesselDetail.tsx & pages/ops/OpsVesselDashboard.tsx if unused (tests reference OpsVesselDashboard in
  server/opsEquipmentWording.test.ts line 19 and contractVesselLink.test.ts references VesselDetail.tsx + App route -> UPDATE THOSE TESTS).
- Tests to update: server/contractVesselLink.test.ts (expects navigate(`/vessels/${a.vesselId}`) and the /vessels/:id route),
  server/opsEquipmentWording.test.ts (reads OpsVesselDashboard.tsx).
- Run: npx tsc --noEmit ; npx vitest run  (1115 tests baseline)
- Vessel ids for screenshots: APPALOOSA=2430131, CHA CHA CHA=5340001, contract 1 = /ops/contracts/1

## Progress update (after wiring pass 1)
DONE: VesselLink.tsx (VesselLink + VesselBadge using useVesselModal), InvoicesTable (VesselBadge, local dialog removed,
 disableVesselDialog now just suppresses the click), Invoices.tsx vessel view (VesselLink), OpsContractDetail (openVessel on
 Vessels tab + financials rows), Vessels.tsx (uses context + ?vessel= deep link effect), RecordDetailsPanel (openVessel;
 related vessels open modal; button label "Open vessel card").
TODO NEXT: OpsAssets.tsx line ~253 vessel cell -> VesselLink (row has a.vesselId); OpsCertificates.tsx ~309 (server now
 returns vesselId on cert rows + upcomingRenewals); check OpsDashboard/GroupDetail/CustomerDetail/GlobalSearch for vessel names;
 delete pages/VesselDetail.tsx + pages/ops/OpsVesselDashboard.tsx (now unrouted) and update tests
 server/contractVesselLink.test.ts + server/opsEquipmentWording.test.ts (reads OpsVesselDashboard.tsx).

## Progress pass 2 (all wiring done, TS clean)
Wired to modal: InvoicesTable (VesselBadge), Invoices.tsx (vessel view VesselLink + credit-note cell),
 OpsContractDetail (Vessels tab + financials), Vessels.tsx (context + ?vessel= deep link), RecordDetailsPanel,
 OpsAssets, OpsCertificates (server now returns vesselId), GroupDetail (per-vessel breakdown name),
 GlobalSearch (vessel hit -> goVessel), AddressBook (vessel row -> openVessel).
App.tsx: VesselModalProvider mounted; /vessels/:id and /ops/vessel/:id redirect to /vessels?vessel=<id>.
Modal content (VesselDetailDialog): header badges (IMO/type/flag/contract count), 4 KPI cards, info grid
 (owner group, IMO/type/flag, notes, invoiced-by, Prime 247 contracts), grouped Products via VesselProductsTable,
 invoices via InvoicesTable(disableVesselDialog).
REMAINING: delete now-unrouted pages/VesselDetail.tsx and pages/ops/OpsVesselDashboard.tsx; update tests
 server/contractVesselLink.test.ts (expects navigate('/vessels/:id') + VesselDetail mount + product internals in
 VesselDetail.tsx) and server/opsEquipmentWording.test.ts (reads OpsVesselDashboard.tsx lines 83-109);
 add a new test pinning modal-everywhere; run vitest; screenshot; checkpoint; update todo.md.
