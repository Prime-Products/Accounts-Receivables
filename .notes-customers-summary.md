# Customers page summary cards — implementation notes (26/7)

User request: show the group-view KPI cards on the Customers (group list) page as TOTALS across all groups.

## Cards (top row, same as GroupDetail lines 449-525):
1. Open Balance (fmtEur totals.openBalance) + byCurrency subtitle
2. Overdue (red) + N overdue invoice(s) + EOM: €X (orange)
3. AI Forecast (this month): Initial / Current / collected / remaining (emerald)
4. Paid (this month) (emerald) = sum of forecast.collected
5. Remain to Collect (this month) (amber) = sum of forecast.remaining
6. Turnover (up to day) (blue) + last year + % comparison

## Aging row (like GroupDetail 528-562):
Current (not due), 0-30, 31-60, 61-90, 91-120, 120+ — each: amount EUR + invoice count.

## Data source approach:
- customers.groups already returns per-group: openBalance, overdueBalance, overdueEomBalance, overdueCount, openByCurrency, turnoverYtd, turnoverLastYear (see ar.ts ~452-545).
- Frontend Customers.tsx can sum these over filtered rows so totals respect active filters (status/confirmation).
- Forecast: need per-group forecast fields (expectedAmount/initial/collected/remaining). Check whether groups payload includes forecast; GroupDetail uses `groupForecast` from a separate query. For Customers totals, add aggregate fields to the groups procedure OR use forecast.list query summed on the client.
- Aging buckets: NOT in groups payload currently. Need backend to compute aging totals; simplest: extend customers.groups response with per-group aging buckets {current, b0_30, b31_60, b61_90, b91_120, b120p} amounts+counts computed from invoices already loaded in that procedure (it iterates invoices per group ~line 486).

## UI placement:
- Customers.tsx: cards row above the filter bar/table, grid-cols-2 lg:grid-cols-3 xl:grid-cols-6 gap-3, then Aging card.
- Sum over `filtered` groups list so filters affect totals (per my message to user).
- fmtEur + fmtByCurrency from client/src/lib/format.ts.

## Existing helpers:
- GroupDetail bucketOf(dueDate, now): <=30 "0-30", <=60 "31-60", <=90 "61-90", <=120 "91-120", else "120+"; null if not due.
- outstanding(inv) server-side helper exists in ar.ts.

## Backend done (26/7):
- groups payload now includes: forecastInitial, hasForecast, aging {current, currentCount, buckets{0-30,31-60,61-90,91-120,120+ each amount+count}}
- Frontend next: sum over filtered groups in Customers.tsx.
