# Working notes — 29 Jul (contact-on-task fix + risk score column)

## CURRENT TASK (29/7 evening): Promise resolution flow rework
User-approved design:
- Kept: green "Kept" badge until end of month; auto-reset to Not Contacted from next month (do NOT show Not Contacted immediately — user thought contact never happened).
- Not Confirmed (Broken): red badge stays until user picks a next action. After pressing "Not Confirmed" in TaskDetailDialog (and Tasks.tsx dialog), open a "Next Action" dialog with 3 options:
  1. Follow-up call w/ date → sets Pending Follow-up + creates follow-up task (use existing updateConfirmationStatus mutation w/ status "Pending Follow-up" + followUpDate)
  2. New promise w/ amount+date → status Confirmed + createGroupPromise (updateConfirmationStatus w/ "Confirmed", amount, followUpDate=promisedDate)
  3. Escalate → change group watch status (existing watch status mutation — check name, likely customers.setWatchStatus)
- Badge click when status Broken should also open Next Action dialog (ConfirmationBadgeButton in Customers.tsx).

### Key facts discovered

### PROGRESS (29/7 ~19:50)

### NEW USER REQUEST (29/7 ~20:00)
- Remove the 3-dot actions column from the groups list (Customers.tsx groups table).

### NEW USER REQUEST (29/7 ~20:15): Under Review → Critical

### BUG (29/7 ~20:40): "Task not found" dialog after promise Kept
User report: marking a promise Kept (or any linked task completed/cancelled) then clicking the badge shows "Task not found" dialog.
Diagnosis so far:
- TaskDetailDialog (client/src/components/TaskDetailDialog.tsx) queries `trpc.tasks.list` (line 38) and finds task by id (line 39). tasks.list likely returns only OPEN tasks → completed task disappears → `!task` → "Task not found" fallback at line 94-102.
- Root cause A: after marking Kept via setPromiseStatus, the dialog STAYS OPEN; tasks.list invalidates and the task vanishes → dialog flips to "Task not found". FIX: close dialog (onOpenChange(false)) in setPromiseStatus onSuccess (Kept path; Broken path opens NextActionDialog — also close main dialog), and in setStatus onSuccess when status completes/cancels the task.
- Root cause B: Customers.tsx line 799 passes `(g as any).confirmationTaskId` to ConfirmationBadgeButton; backend ar.ts line ~709 confirmationTask may be stale until customers.groups refetch. Clicking badge with stale id → dialog "Task not found". Also GroupDetail.tsx line 437.
- Better UX: instead of showing "Task not found" dialog, auto-close + toast, or in ConfirmationBadgeButton only open dialog when task exists.
- tasks.list: check server side whether it includes completed tasks (server/routers/ar.ts tasks router). If it includes completed, root cause A is wrong — verify.
TODO items are in todo.md under "Bug: Task not found".

CONFIRMED DIAGNOSIS (29/7):
- tasks.list with NO input returns ALL statuses (db.listTasks no filter) → completed tasks ARE in the list. So stale taskId still resolves.
- Actual flow of bug: user opens TaskDetailDialog from badge → clicks Kept → updatePromise completes the linked task server-side → onSuccess invalidates tasks.list → dialog STAYS OPEN. Wait: completed task is still returned by tasks.list, so task found... BUT TaskDetailDialog on Tasks page? Tasks.tsx filters? Hmm. Alternative: TaskDetailDialog is opened from badge with taskId=confirmationTaskId. After Kept, customers.groups refetch → badge now "Kept", confirmationTaskId=null (backend derives from OPEN tasks only, line ~709 confirmationTask from openAutoTasks). taskOpen still true; TaskDetailDialog receives NEW prop taskId=null while open → task=null → "Task not found" INSIDE the open dialog.
- FIX: (1) In TaskDetailDialog, keep last non-null taskId in state (latch) so prop flip to null doesn't blank it; (2) close the dialog after Kept/Broken mutation success (Kept → close + toast; Broken → close + NextActionDialog already opens); (3) graceful fallback text still fine as safety.
Workflow becomes: Normal → Problematic → Critical → On Hold → Legal.
DB: group_watch_status enum already contains "Critical"; UPDATE ran (0 Under Review rows existed). NOTE: normalizeStoredStatus previously mapped legacy "Critical"→Problematic — must now map "Under Review"→Critical instead and keep "Critical" as first-class.
Files to change (grep "Under Review"):
- server/lib/statusWorkflow.ts: GROUP_STATUSES, normalizeStoredStatus, resolveGroupStatus manual-wins list, comments
- server/db.ts:651 setGroupWatchStatus type union
- server/routers/ar.ts:1271 z.enum for setWatchStatus; :2684 dashboard underReview count; :668 comment
- server/lib/arLogic.ts:353,421,426 rating comment + score branch (treat "Critical" like old Under Review = 3 pts)
- server/statusWorkflow.test.ts:34-36 test rename
- server/creditRating.test.ts:73 onHoldStatus "Under Review" (proposals enum — different table, keep? proposals onHoldStatuses enum in drizzle/schema.ts:238 KEEPS Under Review — only watch-status workflow renamed; creditRating input uses onHoldStatus string → update branch to accept both)
- client/src/components/WatchStatusSelect.tsx: colors, item, title, type union
- client/src/components/NextActionDialog.tsx: watchStatus union, SelectItem, description text
- client/src/pages/Customers.tsx:328 "under-review" filter → "critical"; :563 SelectItem; :776 badge color branch
- drizzle/schema.ts:330 comment only (watchStatuses enum already has Critical). onHoldStatus enums for proposals stay unchanged (separate on-hold proposal workflow).
- GroupDetail.tsx has no Under Review refs (uses WatchStatusSelect + watchStatus letter badges in Customers).
Watch badge letter: Customers.tsx uses charAt(0) — "C" for Critical (distinct from Problematic "P", Legal "L") OK; check color branch order.

### FAILING TEST monthRollover (isolated failure)
- Failure point moved to line 70: expect((stale as any)?.taskOverdue).toBe(true) — got false.
- Cause: my updateConfirmationStatus change created an open promise on the FIRST Confirmed call (future date). Second Confirmed call (past date) → rescheduleGroupPromise? No — test doesn't pass reschedulePromiseId; my code: findOpenGroupPromise → exists (future date one) → no new promise. getConfirmationStatus taskOverdue: status Confirmed → looks for open promise check task via openPromise (future-dated promise task, not overdue) → linked found → isTaskOverdue(linked)=false. Before my change there was NO promise/task → fallback followUpDate<now → true.
- Fix options: in updateConfirmationStatus, when an open promise exists and followUpDate given, RESCHEDULE the open promise to the new date/amount (rescheduleGroupPromise). That aligns task due date with new followUpDate → past date → task overdue true. This matches user intent (badge date = promise date).
- DONE backend: schema enum + DB ALTER applied ("Kept" added to group_confirmation_status.status enum, migration drizzle/0031_tranquil_mandrill.sql).
- DONE backend: isConfirmationStale(status, followUpDate, now, updatedAt) — Kept stale iff isFromPreviousMonth(updatedAt); both callers (effectiveConfirmation ~line 114, getConfirmationStatus ~line 3535) pass updatedAt.
- DONE backend: updatePromise Kept → conf status "Kept" + promise amount; Broken → "Broken" amount 0.
- DONE backend: updateConfirmationStatus "Confirmed" with amount>0 now creates group promise via createGroupPromise (if no open promise) — used by NextActionDialog new-promise path.
- DONE frontend: format.ts Kept color (emerald solid) + label "Paid — Promise Kept".
- DONE frontend: NextActionDialog.tsx created (menu → followup/promise/escalate; uses trpc.calls.updateConfirmationStatus + trpc.customers.setWatchStatus).
- DONE frontend: TaskDetailDialog wired (fragment wrapper + nextActionGroup state).
- IN PROGRESS: Tasks.tsx wiring — added mutation hook + dialog at end; STILL NEED: import NextActionDialog + useState nextActionGroup declaration near openTask (line ~113).
- REMAINING: openTask declared AFTER setPromiseStatus hook in Tasks.tsx? openTask at line 113, hook at ~63 — closure over openTask var declared later with const → TDZ risk? It's fine at runtime (callback runs after render) but TS hoisting: const openTask used inside onSuccess closure defined earlier in same scope — JS closures allow this since execution happens later. OK.
- REMAINING: update promiseConfirmationSync.test.ts Kept expectation → "Kept" + amount 500; run all tests; screenshot verify; checkpoint; push github main.
- `confirmationStatuses = ["Not Contacted", "Confirmed", "Pending Follow-up", "Broken"]` in drizzle/schema.ts line ~454; MySQL enum column groupConfirmationStatus.status. ADD "Kept" requires ALTER TABLE enum change via webdev_execute_sql + schema.ts update.
- `isConfirmationStale(status)` in server/routers/ar.ts (~line 79): Not Contacted → stale, everything else persists. For "Kept" add: stale if updatedAt is from a previous month (pass updatedAt; isFromPreviousMonth helper exists right below).
- `effectiveConfirmation` (~line 107) uses isConfirmationStale(row.status, row.followUpDate) — needs updatedAt param for Kept expiry.
- forecast.updatePromise mutation (~line 2760): currently on Kept/Broken sets conf row: Broken→"Broken", Kept→"Not Contacted" (CHANGE Kept→"Kept").
- Badge component: ConfirmationBadgeButton in Customers.tsx (used at ~line 887); has status colors; need "Kept" green style + Broken click → NextActionDialog.
- TaskDetailDialog.tsx: setPromiseStatus mutation at ~line 47; Kept/Not Confirmed buttons at ~lines 257/266. After Broken success → open NextActionDialog (needs group name — task.promise? check available fields; task has customerId; maybe task.group or parse).
- Tasks.tsx has its own detail dialog with same buttons (check ~line 417 area).
- updateConfirmationStatus mutation (~line 3566) handles all statuses + cleanupStatusArtifacts + creates follow-up task for Pending Follow-up and promise for Confirmed (verify: line 3599+ creates task; check if Confirmed creates promise via createGroupPromise).
- Watch status mutation: grep "setWatchStatus\|updateWatchStatus" in server/routers/ar.ts.
- Tests: server/promiseConfirmationSync.test.ts must be UPDATED (Kept now expects "Kept" badge, not "Not Contacted").
- Test conventions: appRouter.createCaller(createAuthContext()) w/ user id 1 role "user", purgeTestCustomers(["Prefix%"]) from ./testCleanup, calls.logCall({group, customerId, outcome:"Reached", confirmationStatus:"Confirmed", confirmationAmount, promisedDate}).
- Checkpoints: latest 7bf04794 pushed to github main. Live domain: ar-accounts-evjqbcnz.manus.space (user publishes manually).

## Feature 1: Follow-up task shows call contact — DONE (needs checkpoint+push)
- server/routers/ar.ts:
  - `upsertFollowUpTask` accepts `contactName?`; description now includes ` Contact: <name>.` before Notes
  - `createGroupPromise` accepts `contactName?`; promise task description includes ` Contact: <name>.`
  - `logCall` passes `input.contactName` to both helpers
- client/src/components/TaskDetailDialog.tsx: Contact row parsed from description regex `/Contact: ([^.·]+)[.·]/` with User icon
- client/src/pages/Tasks.tsx: same Contact row in its own detail dialog; imported `User as UserIcon` from lucide
- server/followUpContact.test.ts: 2 tests PASSING (follow-up + promise task contain "Contact: <name>"); afterAll purges "FollowUpContact Test%"
- tsc clean.

## Feature 2 (REVERSED 29/7): user does NOT want risk score anywhere
- User cancelled the Risk Score column AND the Reset sort button.
- DONE: removed Reset sort buttons + ?sort= URL preset from Customers.tsx (RotateCcw import dropped).
- DONE: customers.groups default sort changed to overdueBalance desc (tie → openBalance desc) at ~line 747 of server/routers/ar.ts.
- callList procedure still uses computeCallPriority internally but has NO client consumer (verified: no grep hits in client/). Left in place.
- Starbulk/Minerva investigation (obsolete): Minerva scored 162,827 vs Starbulk 2,017 — backend order was correct; user perception issue came from Problematic badge. Irrelevant now.

## Previous session facts
- Reset sort button (amber, RotateCcw icon) on Customers page appears when groupSort.key/companySort.key !== null; ?sort= URL param presets group sort (valid keys list in Customers.tsx useState init).
- Checkpoint 0c5234ba pushed to github (hovangr22/Accounts-Receivables main).
- Greek-speaking user; push to GitHub after each feature; test-data must purge itself (server/testCleanup.ts purgeTestCustomers).
