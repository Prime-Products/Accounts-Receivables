# Resizable columns & modals rollout — working notes (28/7)

## Infrastructure (DONE)
- `client/src/components/ResizableTable.tsx`: `useResizableColumns(storageKey, defaults)` returns
  `{ widths, style(col), startResize, reset, totalWidth, resizingCol }`. Persists to localStorage
  under `col-widths:<key>`. `<ColResizer col="x" api={cols} />` = drag handle placed inside a
  `TableHead` that has `relative` class + `style={cols.style("x")}`. Double-click handle = reset all.
  Table itself needs: `<Table className="table-fixed" style={{ width: cols.totalWidth, minWidth: "100%" }}>`.
- `client/src/components/ResizableDialogContent.tsx`: `<ResizableDialogContent storageKey="..." defaultWidth={} defaultHeight={}>`
  drop-in replacement for `DialogContent`; drag e/w/s/sw/se edges, persists `dialog-size:<key>`, double-click reset.

## Applied so far (DONE)
- InvoicesTable (`client/src/components/InvoicesTable.tsx`): storage keys `invoices` / `invoices-nocust`
  (with/without Customer col). Cells with truncation: customer + vessel cells use `overflow-hidden` + truncate.
- Vessels page (`client/src/pages/Vessels.tsx`): key `vessels`; plain heads IMO + maxDaysOverdue wrapped manually.

## Applied (cont.)
- Customers page: groups + companies tables done (TOTAL colSpan fixed 4→3, name truncate).
- Contacts done (key `contacts`). Tasks done (key `tasks`) — BUG: task/customer text overlaps neighbors, need overflow-hidden truncate on body cells (fix in progress).
- WireTransfers component: both tables done (keys `wire-transfers`, `wire-payments`).
- Dialogs converted to ResizableDialogContent: VesselDetailDialog, GroupNotesDialog, LogCallDialog, NewTaskDialog, TaskDetailDialog, SendEmailDialog, GroupAiSummaryDialog, AllocateWireTransferDialog.

## Remaining TODO
- Fix Tasks body cell truncation (screenshot showed overlap).
- Lower priority small tables: Contracts installments (nested), Reports, Team, Settings, Forecast, GroupDetail byBranch/companies subtables — skip unless asked.
- After: tsc, screenshots, vitest, todo.md, checkpoint, deliver in Greek.

## Test suite status
- 193 tests; 2 known flaky (confirmationStatus parallel-run) pass in isolation.
