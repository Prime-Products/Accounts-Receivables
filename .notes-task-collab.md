# Working notes — Internal collaboration via tasks (user request 29/7)

## State
- Red overdue badge feature DONE + checkpointed (version aa9e654f).
  - isConfirmationStale never date-resets; isTaskOverdue() helper added in server/routers/ar.ts (~line 106)
  - groups list exposes confirmationTaskOverdue; groupDetail exposes confirmationTaskOverdue; getConfirmationStatus exposes taskOverdue
  - Customers.tsx ConfirmationBadgeButton + GroupDetail.tsx GroupConfirmationBadge take taskOverdue prop → red bg-red-100 text-red-700 border-red-300 + AlertTriangle icon + "Overdue task" hint
  - monthRollover.test.ts updated (expired promise stays Confirmed + taskOverdue=true)
  - Known flakes: confirmationStatus.test.ts 2 tests fail in parallel run, pass in isolation

## In progress: task collaboration
- DB: task_comments + task_invoices tables CREATED in DB (via webdev_execute_sql) and added to drizzle/schema.ts (taskComments, taskInvoices exports). Migration file drizzle/0029_misty_payback.sql generated (already applied manually).
- db.ts helpers added: getTask, listTaskComments, addTaskComment, deleteTaskComment, listTaskInvoices, listAllTaskInvoices, addTaskInvoices
- tasksRouter at server/routers/ar.ts line ~2394: create (has assigneeId input already), list (returns assigneeName), assign, updateStatus, runEngine
- teamRouter at ~2489 with list/create/update/remove/workload

## DONE (backend + frontend)
- tasksRouter.create accepts invoiceIds[] → db.addTaskInvoices. list returns attachedInvoices[], creatorName, createdByMe. comments/addComment procedures added. db.listUsers() added.
- NewTaskDialog: new props attachInvoices, defaultTitle, defaultDescription; shows attached invoices box; sends invoiceIds on create. Assignee picker already existed (TeamMemberSelect).
- TaskCommentsThread component created (client/src/components/TaskCommentsThread.tsx) — list + composer, Ctrl+Enter submit.
- TaskDetailDialog: shows attachedInvoices box + TaskCommentsThread.
- Tasks.tsx: scope Tabs (All / Created by me / Assigned (from others)), attached-invoice count chip + "by {creator}" under title, attachedInvoices box + comments in detail dialog.
- InvoicesTable: enableSelection (default true) — checkbox column, select-all, floating bar "N invoice(s) selected → Send to colleague / Clear", opens NewTaskDialog with prefilled title/description/attachInvoices; InvoiceRowData now has optional customerId.
- tsc clean at this point.

## TODO remaining
1. Ensure invoice list payloads include customerId (check invoices.list procedure ~line 1928 in ar.ts returns customerId; groupDetail invoices; get360) — InvoicesTable uses r.customerId for default task customer; falls back to picker if missing.
2. GroupDetail "Actions" menu: add "Send to colleague" item (NewTaskDialog with customerIds + assignee picker) — optional, ActionsMenu already has New Task; maybe skip. CustomerDetail likewise has NewTaskDialog already.
3. Update todo.md items for collaboration section, run vitest (add small test for tasks.create with invoiceIds + comments), screenshot verify /tasks and /invoices, checkpoint, deliver Greek summary.
4. Known test flakes: confirmationStatus.test.ts 2 tests flaky in parallel (pass isolated).

## Key file locations
- tasksRouter: server/routers/ar.ts ~2394; comments procedures after list.
- Invoices page: client/src/pages/Invoices.tsx (~line 665 InvoicesTable rows={visibleRows}).
- GroupDetail invoices table line ~795; CustomerDetail ~341; VesselDetail 202; VesselDetailDialog 169 (pass enableSelection={false} for vessel dialog to avoid nesting? currently defaults true everywhere).

## Current request (29/7): installment filter toggle everywhere
- DONE: shared component client/src/components/InstallmentToggle.tsx (Tabs 2 options: all / installments)
- DONE: Invoices.tsx — replaced 3-option Select with toggle; contractFilter values now "all"|"installments"; overdueOnly URL param still works (contract=overdue → installments)
- TODO: GroupDetail.tsx invoices list (~line 795) — add toggle state installmentFilter, filter data?.invoices by isContractInstallment (check payload includes flag; groupDetail returns ...i spread so it does), place toggle near agingFilter/statusFilter controls (~line 692 area)
- TODO: CustomerDetail.tsx invoices tab (~line 341) — same toggle
- TODO: mark todo.md items [x], tsc, screenshots (/invoices, group card, customer card), checkpoint, deliver in Greek
- NOTE: prior checkpoints: aa9e654f (red badge), 5c24ef5e (collab), d73fd719 (remove engine button)
