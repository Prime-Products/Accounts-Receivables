# Collector + Account Manager feature — working notes (28/7)

User workflow: each customer has (1) a Collector — assigned to collect the money,
and (2) an Account Manager — handles all cases of the customer. Names of team
members don't matter (user will rename via Team page). Existing accountManagerId
field = Account Manager; NEW collectorId column = Collector.

## Done (backend)
- drizzle/schema.ts: customers.collectorId int column; migration 0028 applied (`ALTER TABLE customers ADD collectorId int`)
- server/db.ts: setGroupCollector(groupName, collectorId); deleteTeamMember also detaches collectorId
- server/routers/ar.ts:
  - groups list query: collectorByGroup map, returns `collector: {id,name}|null` per group (next to accountManager)
  - groupDetail: returns `collector` (first member company with collectorId)
  - customer 360 (get360): returns `collector`
  - customers.setCollector procedure (mirrors setAccountManager; audits "Set Collector")
  - team.workload: returns collectingCompanies + collectingGroups per member

## Done (frontend)
- AccountManagerControl.tsx: new `role="manager"|"collector"` prop; collector = emerald HandCoins badge, manager = sky UserRound badge; popover explains roles; uses setCollector mutation for collector role
- GroupDetail.tsx: collector control next to manager badge in header
- CustomerDetail.tsx: collector control next to manager badge
- Customers.tsx (groups view): collectorFilter state + dropdown ("All collectors"/"No collector"/members), filter logic, collector name (emerald + HandCoins icon) under manager name in group cell

## Remaining
- DONE: Team page "Collecting" column added; tsc clean; vitest 191/193 pass (2 fails = known confirmationStatus parallel flake, passes isolated 31/31)
- Screenshot verify: /customers OK — "All collectors" filter dropdown visible; Team page OK — Collecting column shows.
- Group card screenshot /groups/NAUTILUS%20MANAGEMENT%20S.A returned 404 "Group not found" (tRPC).
  Cause: screenshots unauthenticated?? No — auth.me returned 200 (id 1). Actual cause: group lookup
  compares trimmed name; DB has [NAUTILUS MANAGEMENT S.A] exactly. EPM GROUP also 404s. BUT user's earlier
  screenshots show group cards working in their browser. Likely the group names in list use different casing/
  trailing dot. NOT caused by collector changes (groupDetail logic untouched except added collector lines).
  ROOT CAUSE FOUND: stored group names contain non-breaking spaces (hex C2A0), e.g.
  "NAUTILUS\u00A0MANAGEMENT\u00A0S.A". My test URLs used regular spaces → no match → 404.
  NOT a regression. Real app links encode the NBSP so user's group cards work fine.
  Screenshot with %C2%A0 to verify the group card.
- todo.md section "Collector + Account Manager assignments (user request 28/7)" — mark items done
- checkpoint + deliver in Greek

## Context notes
- Faye Vanou = team member id 30002, title "Credit Manager", fvanou@primeltd.com
- Memory pressure: chromium was killed earlier to free RAM; avoid browser use
- Dev URL: https://3000-itolm16jmc1tflg97bwq4-205bd477.us1.manus.computer
- Published domain: ar-accounts-evjqbcnz.manus.space
## Verified 28/7 evening
- Group card renders: header shows "No manager" + collector badge (truncated at right edge, second control visible)
- Customers list shows both All managers and All collectors filters
- Team page shows Collecting column
- All items in this feature complete; checkpoint next
