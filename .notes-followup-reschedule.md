# Follow-up reschedule feature — working notes

## Key code locations (ar_app)
- `server/routers/ar.ts`:
  - `calls.logCall` mutation — handles confirmationStatus "Pending Follow-up" (creates follow-up task) and "Confirmed" (promise + reschedulePromiseId support already exists, promiseMode radio in LogCallDialog).
  - `calls.getOpenPromise` query — pattern to copy for `getOpenFollowUpTask`.
  - tasksRouter at ~line 2394: `tasks.list`, `tasks.create`, comments procedures; group-scoped (groupName exposed).
  - `paymentContactsRouter` ~line 3553 (listByGroup, add).
- `client/src/components/LogCallDialog.tsx` — outcomes: Reached / No Answer only. CONFIRMATION_STATUSES = Not Contacted / Confirmed(=Promise to Pay) / Pending Follow-up / Broken(=Not Confirmed Payment). Has openPromise reschedule radio (promiseMode reschedule|new). Group-scoped contacts dropdown with inline add-new form (value "add-new").
- `client/src/components/TaskDetailDialog.tsx` + Tasks page detail dialog in `client/src/pages/Tasks.tsx` — show attached invoices (clickable), comments thread (TaskCommentsThread), Group link.
- Tasks page: scope tabs All/Created by me/Assigned, search by group/customer/title, cancelled hidden unless status filter = Cancelled, overdue open tasks have red row bg.
- Confirmation badges: groups list (Customers.tsx ConfirmationBadgeButton), GroupDetail.tsx GroupConfirmationBadge — red variant when linked open task overdue (confirmationTaskOverdue / taskOverdue flags from backend). Statuses never auto-reset.
- Call list: sorts by tier (Critical/Legal=2 else 0) then risk score; Problematic informational only (`server/lib/arLogic.ts` statusTier/computeCallPriority; tests in `server/creditRating.test.ts`).

## DB tables
- `tasks` (mysql, drizzle/schema.ts), `task_comments`, `task_invoices` exist.
- `group_confirmation_status` table: columns camelCase (groupName, status, followUpDate?, amount, updatedAt...).
- promises table + follow-up tasks auto-created from promises.

## Progress
DONE backend: tasks.rescheduleCount column added (migration 0030 applied). upsertFollowUpTask increments count when date actually changes (it already handled the reschedule for Log Call Pending Follow-up automatically — no extra input needed in logCall). New `calls.getOpenFollowUpTask` ({group}) returns {id,dueDate,rescheduleCount,title}|null. New `tasks.reschedule` ({id,dueDate}) mutation: open tasks only, increments count, syncs group confirmation followUpDate for "(Follow-up: X)" tasks, audits. tasks.list spreads raw row so rescheduleCount flows through automatically. tsc clean.

TODO frontend:
1. LogCallDialog: on "Pending Follow-up" show existing open follow-up info (current date + rescheduled ×N) via getOpenFollowUpTask — informational note that saving moves it to the new date (backend reuses task automatically).
2. Tasks page (client/src/pages/Tasks.tsx) detail dialog + TaskDetailDialog.tsx: editable due date (date input + Save via tasks.reschedule), show "rescheduled ×N" badge when >0. Tasks rows: small ×N badge next to due date.
3. Badges: groups list Customers.tsx ConfirmationBadgeButton + GroupDetail GroupConfirmationBadge — need backend to expose rescheduleCount of linked follow-up task (groups list ~line 682 area, groupDetail ~1026 area) as confirmationRescheduleCount; show "×N" when >=2 maybe. Check exact code before editing.
4. Tests: add server/taskReschedule.test.ts (reschedule increments, same-date no-op, closed task rejected). Push github after checkpoint.

## Env facts
- Tests: `pnpm vitest run` (195 passing before this feature). tsc clean.
- Real group for screenshots: DYNACOM (37 companies), route `/groups/DYNACOM`.
