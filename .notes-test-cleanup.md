# Test-data leak fix (user report 29/7: "TaskLink keeps reappearing")

Root cause: vitest specs run against the real dev DB and several files created
customers without deleting them afterwards.

Done:
- server/testCleanup.ts — shared `purgeTestCustomers(patterns)` helper (deletes
  task_comments, task_invoices, tasks, promises_to_pay, activity_log,
  group_confirmation_status, then customers WITHOUT invoices/receipts).
- confirmationTaskLink.test.ts — has its own afterAll purge for 'TaskLink %' (done earlier).
- bankDetails.test.ts — purge "Test Bank Customer%", "Empty Bank Customer%".
- calls.test.ts — purge "Test Email Customer%".
- DB cleaned: deleted 161 junk customers (Test Bank/Empty Bank/TM %/Wire Transfer Test/Test Email), 0 remaining incl. TaskLink.

All patched: wireTransfers.test.ts both blocks purge their customers + WTA-INV fixtures.
team.test.ts creates team members (not customers) and removes them itself — no patch needed;
the old "TM %" customers were legacy junk, deleted.
Full suite: 195/197 passed; 2 fails are known parallel-run flakes (pass in isolation).
DB verified clean after full test run (junk_remaining=0).
