# Promise/Pending carryover fix — working notes (28/7)

User request: Promise to Pay / Pending Follow-up must NOT reset at month change;
they stay active until their target date passes. Promise date is now MANDATORY.

## Changes done in server/routers/ar.ts
- `isConfirmationStale(status, followUpDate, now)`: Not Contacted → stale; Broken → never stale;
  Confirmed/Pending → stale only when followUpDate < now (no date = stale).
- `effectiveConfirmation(row)` now reads row.followUpDate (not updatedAt).
- `getConfirmationStatus` query uses new signature.
- `logCall`: followUpDate column now stores promisedDate for Confirmed; requires promisedDate
  for Confirmed (amount>0) and followUpDate for Pending Follow-up (BAD_REQUEST otherwise).
- `updateConfirmationStatus`: stores followUpDate for both Pending Follow-up and Confirmed.
- LogCallDialog.tsx: client-side validation requires follow-up date for Pending Follow-up
  (promised date already required for Confirmed).

## Tests state
- server/confirmationStatus.test.ts: all 31 pass in isolation (2 tests updated to pass followUpDate/promisedDate; 2 known parallel flakes may appear in full runs: "reschedules an existing open promise", "cancels the follow-up task").
- server/monthRollover.test.ts: NEEDS UPDATE — 2 tests still assume month-reset semantics:
  1. "a Promise to Pay recorded last month is presented as Not Contacted this month" — must now
     test date-based staleness: promise with future date backdated to last month stays Confirmed;
     promise with past followUpDate becomes Not Contacted.
  2. "a Confirmed group ... exposes confirmationPromiseDate" — updateConfirmationStatus call needs followUpDate param now.
- todo.md has section "Confirmation status month-reset fix (user request 28/7)" + mandatory-date item.

## Remaining
- Rewrite monthRollover tests, run full suite, verify UI (customers list badge dates), checkpoint, deliver.

# Carried-over indicator (28/7, second request) — state
DONE: effectiveConfirmation now returns carriedOver (row.updatedAt in previous UTC month & status active); helper isFromPreviousMonth added in ar.ts (~line 93).
TODO:
1. groups payload (~line 653 conf = effectiveConfirmation(confirmation)): add confirmationCarriedOver: conf.carriedOver to the returned row (near confirmationStatus). Check that `confirmation` row passed includes updatedAt (verify the db helper select).
2. getConfirmationStatus procedure (~line 3375): include carriedOver in returned data (compute via effectiveConfirmation or isFromPreviousMonth on non-stale row).
3. Customers.tsx line ~881 shows "Pay by: {fmtDate(g.confirmationPromiseDate)}" for Confirmed; add a small hint (e.g. amber text "from July"/"carried over") when g.confirmationCarriedOver. Also GroupDetail.tsx ~line 416-420 ConfirmationBadge component (status prop) — add carriedOver prop.
4. Tests: add assertion in monthRollover.test.ts (backdated future-dated promise → carriedOver true in groups payload).
5. Checkpoint + deliver; explain auto-Broken option (user asked for explanation of proposal #1).
User language: Greek. Known flakes: confirmationStatus 2 tests in parallel runs only.
